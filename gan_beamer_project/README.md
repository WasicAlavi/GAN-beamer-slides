GAN Beamer Project

Compile with:
latexmk -pdf -outdir=build main.tex

Assets:
- Put images inside assets/images/
- Put figures inside assets/figures/
- Put animation frames inside animations/frames/
